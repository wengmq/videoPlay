#ifndef _BACKSERVERFUNCTION_H
#define _BACKSERVERFUNCTION_H
#include "CSinDB.h"
#include<string.h>

int login( char *name, char*pwd);
//int login_select_callback(void * data, int col_count, char ** col_values, char ** col_Name);

#endif
